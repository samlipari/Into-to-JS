  function multiply(num1, num2) {
    return num1 * num2;
  }

  let n1 = prompt('Enter the first number:')
  let n2 = prompt('Enter the second number:');
  let product = multiply(n1, n2);
  console.log(`${n1} * ${n2} = ${product}`);
